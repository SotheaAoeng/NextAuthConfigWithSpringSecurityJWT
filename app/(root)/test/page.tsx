const Home = () => {
    const baseUrl= `${process.env.NEXT_PUBLIC_BASE_URL}`;
    return(
        <div className="flex w-full h-screen items-center justify-center">
            <h1 className="text-3xl font-semibold mb-6 text-black text-center">WELCOME TO </h1>
        </div>
    )
}
export default Home